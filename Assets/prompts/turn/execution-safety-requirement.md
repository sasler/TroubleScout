Execution safety requirement: If this request can modify system state, call `authorize_delegated_powershell` with the exact command before delegating that command to `run_delegated_powershell`. For PowerShell cmdlets that support confirmation prompts, include `-Confirm:$false` when appropriate. Do not claim any action was executed unless delegated tool output confirms execution.
